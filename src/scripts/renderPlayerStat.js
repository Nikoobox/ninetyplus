import axios from "axios";
// import renderGameById from './game';

const renderPlayerStat = (playerId) => {
    
    // axios.get(`./games?round=${round}`).then((res) => {
        
    // }).catch(err => {
    //     console.log(err)
    // });
};

export default renderPlayerStat;